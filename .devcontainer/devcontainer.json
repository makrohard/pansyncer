{
  "name": "PanSyncer Codespaces Demo",
  "image": "mcr.microsoft.com/devcontainers/python:3.12-bookworm",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y tmux rlwrap netcat-openbsd build-essential python3-dev && python -m venv .venv && . .venv/bin/activate && pip install -U pip && pip install -e . && chmod +x testlab/start_codespaces_testlab.sh",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  }
}